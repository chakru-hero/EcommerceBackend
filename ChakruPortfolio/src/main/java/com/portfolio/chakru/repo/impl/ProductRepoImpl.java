package com.portfolio.chakru.repo.impl;

import org.springframework.stereotype.Repository;

@Repository
public class ProductRepoImpl {

//    @PersistenceContext
//    private EntityManager entityManager;
//
//    public List<ProductModel> findProductsWithLimitAndSort(String sort, int limit , String category){
//        return entityManager.createQuery("SELECT p FROM product_model p WHERE p.category = "+ category + " ORDER BY p.name " + sort,
//                ProductModel.class).setMaxResults(limit).getResultList();
//    }
}
